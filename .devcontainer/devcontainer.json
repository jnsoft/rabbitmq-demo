{
    "name": "Go Dev Container",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "customizations": {
        "vscode": {
            "settings": {
                "go.gopath": "/go",
                "go.toolsGopath": "/go"
            },
            "extensions": [
                "golang.go",
                "ms-vscode.go"
            ]   
        }
    },
    "forwardPorts": [],
    "postCreateCommand": "go mod tidy && podman run -it --rm --name rabbitmq -p 5672:5672 -p 15672:15672 rabbitmq:4.0-management",
    "remoteUser": "vscode"
    
}
